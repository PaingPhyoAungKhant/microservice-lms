apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: lms-user-public-route
  namespace: asto-lms
  labels:
    app.kubernetes.io/component: user-public
spec:
  parentRefs:
  - name: asto-lms-gateway
  hostnames:
  - asto-lms.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /api/v1/users/swagger  
    backendRefs:
    - name: user-service
      port: 8001

